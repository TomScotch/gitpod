image:
  file: gitpod.dockerfile

tasks:
  - command: service mongodb stop ; mongod --journal --bind_ip 0.0.0.0 --port 27017;
  - command: cd /workspace/gitpod/pritrox/expresscart/ ; npm start --production
  - command: service sshd start ;
ports:
  - port: 22
  - port: 27017
  - port: 3000
    onOpen: open-preview
